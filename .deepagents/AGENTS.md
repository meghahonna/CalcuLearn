# Project Agent Memory

This sandbox starts with an empty `/workspace/template`. Use that
directory as the project root for any new files or repos.

The preinstalled developer agent lives in `/workspace/developer_agent`.
Shared agent memory and skills live in `/workspace/.deepagents/`
(see [agents.md](https://agents.md/) for the file format).
